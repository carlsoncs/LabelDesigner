AUTH_USERNAME=admin
AUTH_PASSWORD=change-me
JWT_SECRET=change-me-to-a-random-string

# Optional: comma-separated list of allowed printer IPs
# If set, the print API will only send to these addresses
# If not set, any valid network IP is allowed (authenticated users only)
# ALLOWED_PRINTER_IPS=192.168.1.100,192.168.1.101
